{
  "name": "@buildd/core",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "db:generate": "drizzle-kit generate:pg",
    "db:push": "drizzle-kit push:pg",
    "db:migrate": "bun run db/migrate.ts",
    "db:studio": "drizzle-kit studio"
  },
  "exports": {
    "./db": "./db/index.ts",
    "./db/*": "./db/*.ts",
    "./worker-runner": "./worker-runner.ts",
    "./config": "./config.ts"
  },
  "dependencies": {
    "@buildd/shared": "workspace:*",
    "@anthropic-ai/claude-agent-sdk": ">=0.2.44",
    "@neondatabase/serverless": "^0.9.0",
    "drizzle-orm": "^0.30.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.4.0"
  }
}
