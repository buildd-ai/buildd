{
  "$comment": "Single source of truth for every Buildd cron trigger. An external scheduler (cron-job.org) calls these with `Authorization: Bearer $CRON_SECRET`. vercel.json declares NO crons: the Vercel-native mechanism does not fire in this project, and routes parked there silently never ran. Run `bun run cron:plan` to preview, `bun run cron:sync` to apply, `bun run cron:check` as a CI gate. Schedules are 5-field cron expressions evaluated in `timezone`. Reconciliation is origin-scoped: only jobs under CRON_TARGET_BASE_URL + /api/cron/ are ever touched, so unrelated projects sharing the scheduler account are safe. `scripts/cron-coverage.test.ts` enforces that every /api/cron/* route appears here exactly once. See docs/cron-jobs.md and docs/specs/external-cron-triggers.md.",
  "timezone": "America/New_York",
  "jobs": [
    {
      "$comment": "Mission/task schedule tick. Hourly, on the hour, all 24 hours. Cadence floor is the finest ENABLED task_schedules row: every one is now hourly-or-coarser, so */30 bought nothing and woke Neon 48x/day instead of 24x. Hour field MUST stay `*` even though nightly is otherwise off \u2014 an hour range here is what silently starved every schedule for 12h a night, and an enabled schedule fires at 06:00. Before making this coarser than a row's cadence, check: isOverdue() allows only interval*2 before flagging a schedule late.",
      "title": "Buildd: Schedules Tick",
      "path": "/api/cron/schedules",
      "schedule": "0 * * * *"
    },
    {
      "$comment": "BT-11/BT-19 stall notification. Route comment recommends every 5 minutes. Staged dark \u2014 no trigger has ever existed for this route.",
      "title": "Buildd: Stall Notify",
      "path": "/api/cron/stall-notify",
      "schedule": "*/5 * * * *",
      "method": "POST",
      "enabled": false
    },
    {
      "$comment": "Distills feedback down-votes/dismissals into memories. Route comment recommends every 1-4 hours. Staged dark \u2014 no trigger has ever existed for this route.",
      "title": "Buildd: Feedback Digest",
      "path": "/api/cron/feedback-digest",
      "schedule": "0 */4 * * *",
      "method": "POST",
      "enabled": false
    },
    {
      "$comment": "Weekly signing-key rotation (cross-app-assertion-grant spec B.3). Rotates when the Active key is older than 30d, so a weekly tick is sufficient. Staged dark \u2014 no trigger has ever existed, meaning signing keys have never rotated.",
      "title": "Buildd: JWKS Rotation",
      "path": "/api/cron/jwks-rotation",
      "schedule": "0 4 * * 0",
      "enabled": false
    },
    {
      "$comment": "Aggregates 7 days of task outcomes for model-routing calibration. Read-only; weekly is enough since the window is 7d. Staged dark \u2014 no trigger has ever existed for this route.",
      "title": "Buildd: Routing Calibration",
      "path": "/api/cron/routing-calibration",
      "schedule": "0 5 * * 1",
      "enabled": false
    },
    {
      "$comment": "Refreshes expiring Codex/Claude/MCP-connector OAuth credentials and verifies Claude liveness. Moved off Vercel-native cron: it never fired there, so an MCP connector credential holding a valid refresh token stayed dead for 15 days until a manual reconnect. Cadence is load-bearing \u2014 apps/web/src/lib/cron-cadence.ts derives the credential sweep's lookahead window from this expression, so changing it here changes the window. Deliberately NOT restricted to daytime: OAuth tokens expire overnight too, and the sweep window is derived from an EVEN interval \u2014 an hour-restricted schedule leaves an 8h night gap the 5h window cannot cover, which is the exact bug this whole change fixed. 6 runs/day is a negligible share of Neon wake time next to the */30 schedules tick.",
      "title": "Buildd: Credential Refresh",
      "path": "/api/cron/codex-token-refresh",
      "schedule": "0 */4 * * *",
      "method": "GET"
    },
    {
      "$comment": "Reminds about connector-blocked tasks and proactively alerts on expired/unrefreshable connector credentials. POST. Moved off Vercel-native cron, which never fired \u2014 the expiry alert added in #1837 could not have worked there. Cadence */5 * * * * -> hourly: a sub-hourly DB-touching job never lets Neon's compute autosuspend, so it bills idle time all day. Daytime only (EST): nothing here needs to fire while nobody is awake to act on a push. (was 0 * * * *)",
      "title": "Buildd: Connector Block + Expiry Notify",
      "path": "/api/cron/connector-block-notify",
      "schedule": "0 7-23 * * *",
      "method": "POST"
    },
    {
      "$comment": "Queue-stall watchdog. Alerts when a task has been pending past the route's 4h threshold and was never successfully claimed, naming the claim gate that is holding it. Threshold and cadence are chosen together: the incident this fixes took 5 DAYS to notice, so the target is 'same working day', and hourly x 4h threshold makes worst-case detection ~5h. Daytime only (EST) for the same reason as the other DB-touching jobs — a sub-hourly job never lets Neon's compute autosuspend, and nobody can act on a stalled-queue push at 03:00; a stall that begins overnight is reported at 07:00. Do NOT make this finer than hourly: the route HTTP-probes connectors for role-gated tasks.",
      "title": "Buildd: Queue Stall Watchdog",
      "path": "/api/cron/queue-stall",
      "schedule": "0 7-23 * * *",
      "method": "POST"
    },
    {
      "$comment": "Releases expired credential leases and alerts the owning team. Moved off the Vercel-native cron (never fired); enabled after review \u2014 its first run acts on a full accumulated backlog, once. Cadence */10 * * * * -> hourly: a sub-hourly DB-touching job never lets Neon's compute autosuspend, so it bills idle time all day. Daytime only (EST): a stale lease overnight costs nothing but a delayed release. (was 0 * * * *)",
      "title": "Buildd: Lease Expiry Guard",
      "path": "/api/cron/lease-expiry-guard",
      "schedule": "0 7-23 * * *",
      "method": "GET"
    },
    {
      "$comment": "Heals workers whose PR merged on GitHub but whose row still says otherwise \u2014 a missed pull_request webhook delivery starves every dependent task until this corrects it, so the cadence has to be within the working hour, not the working day. Merge-state only: the dead-zone sweep (which spawns conflict-resolution tasks) stays on the daily job below. The sweep is capped and rate-limited per run, so a backlog drains across runs. Offset off :00 to stay clear of the schedules tick.",
      "title": "Buildd: PR Merge-State Sweep",
      "path": "/api/cron/pr-reconcile?scope=merge-state",
      "schedule": "20 * * * *",
      "method": "GET"
    },
    {
      "$comment": "Full PR reconcile: merge-state plus the dead-zone sweep, which CREATES conflict-resolution tasks and therefore stays daily. Moved off the Vercel-native cron (never fired); enabled after review \u2014 its first run acts on a full accumulated backlog, once. Moved off 02:00 into the working day (EST). (was 0 2 * * *)",
      "title": "Buildd: PR Reconcile",
      "path": "/api/cron/pr-reconcile",
      "schedule": "0 9 * * *",
      "method": "GET"
    },
    {
      "$comment": "Weekly archive of completed tasks. Moved off the Vercel-native cron (never fired); enabled after review \u2014 its first run acts on a full accumulated backlog, once. Moved off 06:00 into the working day (EST). (was 0 6 * * 1)",
      "title": "Buildd: Task Archive",
      "path": "/api/cron/task-archive",
      "schedule": "0 10 * * 1",
      "method": "GET"
    },
    {
      "$comment": "Post-deploy health watch window: probes the verificationUrl of every release that reached `healthy` within the last 30 minutes. On non-2xx or error, transitions the release to `degraded` and auto-files a workspace task. Every 2 minutes keeps the degradation window tight relative to the 30-minute watch window (at most ~2min latency before detection). Sub-hourly by design \u2014 this is time-critical production monitoring, not a housekeeping sweep.",
      "title": "Buildd: Release Health Check",
      "path": "/api/cron/release-health-check",
      "schedule": "*/2 * * * *",
      "method": "GET"
    }
  ]
}
