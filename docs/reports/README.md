# Reports

**Generated artifacts, not source-of-truth.** Files here are outputs of audits
and drift checks (e.g. `spec_compare`, the spec-sync skill). They are
**rebuildable** and may be stale — always regenerate before trusting them.

Nothing here is a spec or a plan. It must not live under `../specs/` (which
holds living contracts the linter enforces). Keep generated reports here so the
spec namespace stays clean.
