// Mock worker data for the dev fixtures page, in different worker states.
//
// IMPORTANT: all timestamps are derived from the fixed BASE constant below —
// never `Date.now()` / `new Date()`. This file is imported by a client
// component, so any non-deterministic value evaluated at module scope would
// differ between the server render and the client hydration and trigger a
// React hydration mismatch. Milestone times use the `ts` field (number, epoch
// ms) that WorkerActivityTimeline actually reads; using `timestamp` here makes
// the timeline render "Invalid Date".

const MINUTE = 60_000;
const HOUR = 60 * MINUTE;

// Fixed reference instant (2024-07-03T12:00:00.000Z). Deterministic on purpose.
const BASE = 1_720_008_000_000;
const iso = (msAgo: number) => new Date(BASE - msAgo).toISOString();

export const mockWorkers = {
    'waiting-input': {
        id: 'fixture-worker-waiting-input',
        taskId: 'fixture-task-1',
        workspaceId: 'fixture-workspace',
        accountId: 'fixture-account',
        name: 'fixture-worker-abc123',
        branch: 'buildd/fixture-task',
        status: 'waiting_input' as const,
        waitingFor: {
            type: 'question',
            prompt: 'Which authentication method should we use for the new API endpoints?',
            options: ['JWT with refresh tokens', 'Session cookies', 'OAuth2 + PKCE'],
        },
        costUsd: '0.05',
        inputTokens: 5000,
        outputTokens: 2000,
        turns: 12,
        startedAt: iso(HOUR),
        completedAt: null,
        error: null,
        localUiUrl: null,
        currentAction: 'Auth method',
        milestones: [
            { type: 'checkpoint' as const, event: 'session_started', label: 'Session started', ts: BASE - HOUR },
            { type: 'checkpoint' as const, event: 'first_read', label: 'First file read', ts: BASE - 50 * MINUTE },
            { type: 'action' as const, label: 'Project setup', ts: BASE - 50 * MINUTE },
            { type: 'action' as const, label: 'Database schema', ts: BASE - 33 * MINUTE },
        ],
        prUrl: null,
        prNumber: null,
        lastCommitSha: 'abc123',
        commitCount: 3,
        filesChanged: 8,
        linesAdded: 150,
        linesRemoved: 20,
        pendingInstructions: null,
        instructionHistory: [],
        createdAt: iso(HOUR),
        updatedAt: iso(0),
    },
    running: {
        id: 'fixture-worker-running',
        taskId: 'fixture-task-2',
        workspaceId: 'fixture-workspace',
        accountId: 'fixture-account',
        name: 'fixture-worker-def456',
        branch: 'buildd/fixture-task-2',
        status: 'running' as const,
        waitingFor: null,
        costUsd: '0.12',
        inputTokens: 12000,
        outputTokens: 5000,
        turns: 25,
        startedAt: iso(2 * HOUR),
        completedAt: null,
        error: null,
        localUiUrl: 'http://localhost:8766',
        currentAction: 'Implementing API endpoints',
        milestones: [
            { type: 'checkpoint' as const, event: 'session_started', label: 'Session started', ts: BASE - 2 * HOUR },
            { type: 'checkpoint' as const, event: 'first_read', label: 'First file read', ts: BASE - 100 * MINUTE },
            { type: 'checkpoint' as const, event: 'first_edit', label: 'First file edit', ts: BASE - 67 * MINUTE },
            { type: 'action' as const, label: 'Project analysis', ts: BASE - 100 * MINUTE },
            { type: 'action' as const, label: 'Schema design', ts: BASE - 67 * MINUTE },
            { type: 'action' as const, label: 'API scaffolding', ts: BASE - 33 * MINUTE },
        ],
        prUrl: null,
        prNumber: null,
        lastCommitSha: 'def456',
        commitCount: 7,
        filesChanged: 15,
        linesAdded: 420,
        linesRemoved: 85,
        pendingInstructions: null,
        instructionHistory: [],
        createdAt: iso(2 * HOUR),
        updatedAt: iso(0),
    },
    completed: {
        id: 'fixture-worker-completed',
        taskId: 'fixture-task-3',
        workspaceId: 'fixture-workspace',
        accountId: 'fixture-account',
        name: 'fixture-worker-ghi789',
        branch: 'buildd/fixture-task-3',
        status: 'completed' as const,
        waitingFor: null,
        costUsd: '0.25',
        inputTokens: 25000,
        outputTokens: 10000,
        turns: 50,
        startedAt: iso(4 * HOUR),
        completedAt: iso(HOUR),
        error: null,
        localUiUrl: null,
        currentAction: null,
        milestones: [
            { type: 'checkpoint' as const, event: 'session_started', label: 'Session started', ts: BASE - 4 * HOUR },
            { type: 'checkpoint' as const, event: 'first_read', label: 'First file read', ts: BASE - 217 * MINUTE },
            { type: 'checkpoint' as const, event: 'first_edit', label: 'First file edit', ts: BASE - 167 * MINUTE },
            { type: 'checkpoint' as const, event: 'first_commit', label: 'First commit', ts: BASE - 117 * MINUTE },
            { type: 'checkpoint' as const, event: 'task_completed', label: 'Task completed', ts: BASE - HOUR },
            { type: 'action' as const, label: 'Analysis complete', ts: BASE - 200 * MINUTE },
            { type: 'action' as const, label: 'Implementation done', ts: BASE - 133 * MINUTE },
            { type: 'action' as const, label: 'Tests passing', ts: BASE - 83 * MINUTE },
            { type: 'action' as const, label: 'PR created', ts: BASE - HOUR },
        ],
        prUrl: 'https://github.com/example/repo/pull/123',
        prNumber: 123,
        lastCommitSha: 'ghi789',
        commitCount: 15,
        filesChanged: 25,
        linesAdded: 800,
        linesRemoved: 150,
        pendingInstructions: null,
        instructionHistory: [],
        createdAt: iso(4 * HOUR),
        updatedAt: iso(HOUR),
    },
    failed: {
        id: 'fixture-worker-failed',
        taskId: 'fixture-task-4',
        workspaceId: 'fixture-workspace',
        accountId: 'fixture-account',
        name: 'fixture-worker-jkl012',
        branch: 'buildd/fixture-task-4',
        status: 'failed' as const,
        waitingFor: null,
        costUsd: '0.08',
        inputTokens: 8000,
        outputTokens: 3000,
        turns: 15,
        startedAt: iso(90 * MINUTE),
        completedAt: iso(30 * MINUTE),
        error: 'Build failed: TypeScript compilation error in src/api/routes.ts',
        localUiUrl: null,
        currentAction: null,
        milestones: [
            { type: 'action' as const, label: 'Started', ts: BASE - 83 * MINUTE },
        ],
        prUrl: null,
        prNumber: null,
        lastCommitSha: 'jkl012',
        commitCount: 2,
        filesChanged: 5,
        linesAdded: 100,
        linesRemoved: 10,
        pendingInstructions: null,
        instructionHistory: [],
        createdAt: iso(90 * MINUTE),
        updatedAt: iso(30 * MINUTE),
    },
};

export type FixtureState = keyof typeof mockWorkers;
