# Test environment configuration
# Copy to .env.test and fill in values
#
# BUILDD_TEST_SERVER is REQUIRED — tests will skip without it.
# This prevents accidental runs against production.

BUILDD_TEST_SERVER=http://localhost:3000  # or Vercel preview URL
BUILDD_API_KEY=bld_...
LOCAL_UI_URL=http://localhost:8766
